# StudEats Production Environment Configuration for Render
# Copy these settings to your Render Dashboard Environment Variables

# ===== CRITICAL SETTINGS =====
APP_NAME=StudEats
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:YOUR_GENERATED_KEY_HERE

# Generate APP_KEY locally with: php artisan key:generate --show
# Then copy the output (including "base64:") to Render dashboard

# ===== APPLICATION SETTINGS =====
APP_URL=https://your-service-name.onrender.com
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# ===== DATABASE SETTINGS =====
# Render automatically provides DATABASE_URL for PostgreSQL
# These settings will be automatically parsed from DATABASE_URL
DB_CONNECTION=pgsql

# ===== CACHE & SESSION SETTINGS =====
CACHE_STORE=file
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# ===== QUEUE SETTINGS =====
QUEUE_CONNECTION=database

# ===== EMAIL SETTINGS (REQUIRED for OTP system) =====
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=your_email@gmail.com
MAIL_FROM_NAME=StudEats

# ===== LOGGING SETTINGS =====
LOG_CHANNEL=errorlog
LOG_LEVEL=error
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null

# ===== FILESYSTEM SETTINGS =====
FILESYSTEM_DISK=local
BROADCAST_CONNECTION=log

# ===== LARAVEL OPTIMIZATION =====
APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12
PHP_CLI_SERVER_WORKERS=4

# ===== ADDITIONAL PRODUCTION SETTINGS =====
# These are automatically handled by the startup script
# but can be overridden if needed

# For debugging purposes only (remove after fixing issues)
# APP_DEBUG=true
# LOG_LEVEL=debug

# ===== EXTERNAL API SETTINGS =====
# Add these if you're using external APIs
# USDA_API_KEY=your_usda_api_key
# BANTAY_PRESYO_API_KEY=your_bantay_presyo_api_key

# ===== INSTRUCTIONS =====
# 1. Go to your Render dashboard
# 2. Select your StudEats service
# 3. Go to Environment tab
# 4. Add each variable above as a separate environment variable
# 5. Make sure to replace placeholder values with actual values
# 6. Deploy your service after setting all variables

# ===== CRITICAL NOTES =====
# - Never commit actual sensitive values to git
# - Generate a fresh APP_KEY for production
# - Use a strong email app password for Gmail
# - Test email functionality after deployment
# - Monitor logs for any configuration issues