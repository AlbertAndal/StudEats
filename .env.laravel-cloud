# ============================================
# Laravel Cloud Environment Variables Template
# ============================================
# Copy these to your Laravel Cloud Dashboard
# Environment Variables section
#
# DO NOT commit this file with actual credentials!
# ============================================

# Application Configuration
APP_NAME=StudEats
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:cVdpd4/yHnZtTDWQB+yi351zUDmvNHf/j5pPEgTM4a4=
APP_URL=https://studeats.laravel.cloud

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=error
LOG_DEPRECATIONS_CHANNEL=null

# Database Configuration
# Laravel Cloud will provide these values in the dashboard
DB_CONNECTION=mysql
DB_HOST=<provided-by-laravel-cloud>
DB_PORT=3306
DB_DATABASE=<provided-by-laravel-cloud>
DB_USERNAME=<provided-by-laravel-cloud>
DB_PASSWORD=<provided-by-laravel-cloud>

# Alternative: Use DATABASE_URL if provided
# DATABASE_URL=mysql://username:password@host:3306/database

# Session Configuration
SESSION_DRIVER=database
SESSION_LIFETIME=1440
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Cache Configuration
CACHE_STORE=database

# Queue Configuration
QUEUE_CONNECTION=database

# Broadcasting
BROADCAST_CONNECTION=log

# Filesystem
FILESYSTEM_DISK=local

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=johnalbertandal5@gmail.com
MAIL_PASSWORD=nharujmmwoawzwgp
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=johnalbertandal5@gmail.com
MAIL_FROM_NAME="${APP_NAME}"

# Nutrition API Configuration
NUTRITION_API_URL=https://api.nal.usda.gov/fdc/v1
NUTRITION_API_KEY=Qs1e7LNogSHAQI1GBnHA9vuWx2OwzJceO4FHhEdP

# Vite Configuration
VITE_APP_NAME="${APP_NAME}"

# Redis (Optional - if using Laravel Cloud Redis)
# REDIS_HOST=127.0.0.1
# REDIS_PASSWORD=null
# REDIS_PORT=6379

# ============================================
# SECURITY NOTES
# ============================================
# 1. Generate a NEW APP_KEY for production:
#    php artisan key:generate --show
#
# 2. Use strong database credentials provided
#    by Laravel Cloud dashboard
#
# 3. Keep APP_DEBUG=false in production
#
# 4. Rotate NUTRITION_API_KEY if compromised
#
# 5. Use app-specific mail password (not main)
# ============================================
